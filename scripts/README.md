# Scripts

Utility scripts that require sudo or modify system files. These cannot be managed by stow.

Each subfolder contains a script with its own README.

## Scripts

- **apt-hook/** - Auto-updates pkglist when packages change
- **fix-docker-desktop/** - Fixes Docker Desktop on Sway/Wayland
