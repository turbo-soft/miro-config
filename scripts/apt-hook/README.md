# APT Hook - Auto-update Package List

Automatically updates `pkglist/pkglist.txt` whenever packages are installed or removed via apt.

## Installation

```bash
./install.sh
```

Copies `99update-pkglist` to `/etc/apt/apt.conf.d/`.
